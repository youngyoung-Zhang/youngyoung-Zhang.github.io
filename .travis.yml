language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

script:
  - hexo clean
  - hexo generate

after_script:
  - cd ./public
  - git init
  - git config user.name "youngyoung-Zhang"
  - git config user.email "keng_huo@yeah.net"
  - git add .
  - git commit -m "Update Blog"
  - git push --force --quiet "https://${GITHUB_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog
    
env:
 global:
   - GH_REF: github.com/youngyoung-Zhang/youngyoung-Zhang.github.io
